@page
@model ProductsWebApp.IndexModel


@{
    ViewData["Title"] = "Index";
}


<div class="container">
    <h1 style="text-decoration:underline;">Products List</h1>

    @*<p>
        <a asp-page="Create">Create New</a>
      </p>*@

    <table class="table">
        <thead>
            <tr>
                @{
                    Type type = typeof(APIs.Products);
                    PropertyInfo[] properties = type.GetProperties();
                    foreach (PropertyInfo property in properties)
                    {
                        <th>
                            @property.Name
                        </th>
                    }
                }
                
                @*<th>
                    @Html.DisplayNameFor(model => model.Products[0].Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Products[0].Brand)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Products[0].Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Products[0].Quantity)
                </th>*@
            </tr>
        </thead>
        <tbody>
            @foreach (APIs.Products item in Model.Products)
            {
            <tr>
                @{
                    foreach (PropertyInfo property in properties)
                    {
                        <td>
                            @*@property.Name*@
                            @property.GetValue(item, null)
                        </td>
                    }
                }
                @*<td>
                @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Brand)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>*@
            </tr>
            }
        </tbody>
    </table>
</div>







